type Query {
  user(id: ID!): User
  users(filter: UserFilter): [User!]!
  ping: String!
}

type User {
  id: ID!
  name: String!
  email: String
}

input UserFilter {
  name: String
  active: Boolean!
}
